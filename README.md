# Web-Catalogo

Base do projeto para catálogo online:

- `frontend/`: React + Vite
- `backend/`: API em Go (cadastro/login + catálogo)

Para instruções do backend, veja [backend/README.md](backend/README.md).